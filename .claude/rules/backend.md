---
paths:
  - "{{DIR_BACKEND}}/**"
---

# Regras de backend — {{PROJETO}}

(Exemplo de regra escopada por path — carrega só ao trabalhar nestes arquivos.)

- Siga o padrão de {{FRAMEWORK_BACKEND}} já presente no projeto.
- Migrations idempotentes; leitura degrada se a tabela não existir.
- {{REGRA_BACKEND_EXTRA}}
