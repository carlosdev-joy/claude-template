#!/usr/bin/env bash
# Setup do ambiente — rode no SessionStart (Claude Code na web) para garantir que
# testes/build funcionem. Preencha no /bootstrap-projeto com os comandos reais.
set -e

# Exemplos (descomente/ajuste conforme a stack):
# npm ci --prefix ui-react || true
# pip install -r requirements.txt || true
# make setup || true

echo "[setup] Ajuste scripts/setup.sh com as dependências do projeto."
